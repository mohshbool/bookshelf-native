import { StyleSheet } from 'react-native'

const gloablStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
  input: {
    borderWidth: 1,
    borderColor: '#7a7a7a',
    padding: 0.8
  }
})

export default gloablStyles